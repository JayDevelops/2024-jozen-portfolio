import type {Metadata} from "next";

export default function PhotosPage() {
    return (
        <div>
            Photos Page...
        </div>
    )
}

export const metadata: Metadata = {
    title: "Videos",
    description: 'Jozen\'s photos.',
}